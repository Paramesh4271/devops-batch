Launch a Linux EC2 instance
2.Create an Instance in us-east-1 (N. Virginia) region with an Ubuntu OS and install apache for making them web servers 
3. Change the default website with a hello world page
4. Create a Web Server AMI with Apache 2 server running in it, and launch the instances in two more regions with the same configuration.
5. Create a EBS volume with 20 GB of storage and attach it the created EC2 instance 
